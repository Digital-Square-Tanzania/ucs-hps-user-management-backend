# UCS HPS User Management Backend
A backend for managing users in UCS thought OpenMRS team module API.

This backend application is used to link the frontend user management application and the OpenMRS API. It is also used to authenticate users that are allowed to manage teams and accounts in UCS.
